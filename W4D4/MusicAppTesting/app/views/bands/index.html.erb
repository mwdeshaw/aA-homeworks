<h1>THE BANDS</h1>

<ul class='main-list'>
  <% @bands.each do |band| %>
    <li><a href="<%= band_url(band) %>"><p><%= band.name %></p></a></li>
  <% end %>
</ul>


<br>
<h4>
<a class='button' href="<%= new_band_url %>" >New band</a>
</h4>